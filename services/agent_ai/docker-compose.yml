version: '3.8'

services:
  ai-reply-backend:
    build: .
    container_name: ai-reply-backend
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file:
      - .env
    dns:
      - 8.8.8.8
      - 8.8.4.4
    restart: always
